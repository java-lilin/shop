db: shop
server:
  port: 8089
  servlet:
    context-path: /common
    session:
      cookie:
        name: ROBOT_MANAGE_SID
    tomcat:
      max-connections: 10000
      max-http-form-post-size: 500MB
      max-swallow-size: 500MB
      max-threads: 1000
      min-spare-threads: 500
spring:
  servlet:
    multipart:
      maxFileSize: 500MB
      maxRequestSize: 5000MB
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  main:
    allow-bean-definition-overriding: true
  profiles:
    include: druid,mybatisplus
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/${db}?zeroDateTimeBehavior=convertToNull&characterEncoding=UTF-8&useSSL=false&rewriteBatchedStatements=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  rabbitmq:
    host: 117.78.10.200
    port: 42803
    username: admin
    password: 1qazxsw2
    publisher-returns: true
    virtual-host: /
    listener:
      simple:
        #        自动确认消费
        acknowledge-mode: manual
        concurrency: 5
        max-concurrency: 10